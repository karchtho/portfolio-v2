services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    ports:
      - "4200:4200"
    environment:
      - NODE_ENV=development
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/public:/app/public
      - /app/node_modules
    # Pas de depends_on backend pour l'instant

# Backend commenté tant que tu n'en as pas besoin
#  backend:
#    build:
#      context: ./backend
#      dockerfile: Dockerfile.dev
#    ports:
#      - "3000:3000"
#    environment:
#      - NODE_ENV=development
#    volumes:
#      - ./backend/src:/app/src
#      - /app/node_modules