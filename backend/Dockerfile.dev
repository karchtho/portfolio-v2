FROM node:24-alpine

WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm install

# Copy TypeScript config (needed for ts-node)
COPY tsconfig.json ./

# Source code is mounted via volume for hot-reload

EXPOSE 3000

CMD ["npm", "run", "dev"]